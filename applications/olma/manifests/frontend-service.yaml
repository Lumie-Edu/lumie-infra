apiVersion: v1
kind: Service
metadata:
  name: olma-frontend
  namespace: olma
spec:
  type: ClusterIP
  selector:
    app: olma-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
