apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: nextjs-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
    - name: build-args
      description: Docker build arguments (comma-separated)
      default: ""
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: $(tt.params.repo-name)-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: nextjs-build-deploy
          app: $(tt.params.repo-name)
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: nextjs-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: $(tt.params.repo-name)
          - name: context-dir
            value: ./nextjs
          - name: image-prefix
            value: web-apps/
          - name: build-args
            value: $(tt.params.build-args)
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/$(tt.params.repo-name)/helm-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: lumie-document-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: $(tt.params.repo-name)-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: nextjs-build-deploy
          app: $(tt.params.repo-name)
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: nextjs-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: $(tt.params.repo-name)
          - name: context-dir
            value: .
          - name: image-prefix
            value: web-apps/
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/lumie/document/helm-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: fastapi-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: $(tt.params.repo-name)-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: fastapi-build-deploy
          app: $(tt.params.repo-name)
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: fastapi-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: $(tt.params.repo-name)
          - name: context-dir
            value: ./fastapi
          - name: image-prefix
            value: web-apps/
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/$(tt.params.repo-name)/helm-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
# Lumie Backend Monolith TriggerTemplate
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: lumie-backend-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: lumie-backend-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: lumie-springboot-build-deploy
          app: lumie-backend
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 45m
        pipelineRef:
          name: lumie-springboot-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: service-name
            value: lumie-backend
          - name: context-dir
            value: .
          - name: dockerfile-path
            value: Dockerfile
          - name: image-prefix
            value: applications/
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/lumie/backend/lumie-backend/common-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: lumie-frontend-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: lumie-frontend-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: nextjs-build-deploy
          app: lumie-frontend
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: nextjs-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: lumie-frontend
          - name: context-dir
            value: .
          - name: image-prefix
            value: applications/
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/lumie/frontend/common-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
# Lumie AI TriggerTemplates
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: lumie-grading-svc-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: grading-svc-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: fastapi-build-deploy
          app: grading-svc
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: fastapi-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: grading-svc
          - name: context-dir
            value: ./services/grading
          - name: image-prefix
            value: applications/
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/lumie/ai/grading-svc/common-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: lumie-report-svc-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: report-svc-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: fastapi-build-deploy
          app: report-svc
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: fastapi-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: report-svc
          - name: context-dir
            value: ./services/report
          - name: image-prefix
            value: applications/
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/lumie/ai/report-svc/common-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: lumie-audio-svc-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: audio-svc-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: fastapi-build-deploy
          app: audio-svc
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: fastapi-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: audio-svc
          - name: context-dir
            value: ./services/audio
          - name: image-prefix
            value: applications/
          - name: update-values
            value: "false"
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: lumie-analysis-svc-build-template
  namespace: tekton-pipelines
spec:
  params:
    - name: git-url
      description: Git repository URL
    - name: git-revision
      description: Git commit SHA
    - name: git-branch
      description: Git branch name
    - name: repo-name
      description: Repository name
    - name: repo-full-name
      description: Full repository name (org/repo)
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: analysis-svc-build-
        namespace: tekton-pipelines
        labels:
          tekton.dev/pipeline: fastapi-build-deploy
          app: analysis-svc
          branch: $(tt.params.git-branch)
      spec:
        timeouts:
          pipeline: 30m
        pipelineRef:
          name: fastapi-build-deploy
        onSuccessfulCompletion: delete
        params:
          - name: git-url
            value: $(tt.params.git-url)
          - name: git-revision
            value: $(tt.params.git-branch)
          - name: app-name
            value: analysis-svc
          - name: context-dir
            value: ./services/analysis
          - name: image-prefix
            value: applications/
          - name: update-values
            value: "true"
          - name: values-file-path
            value: applications/lumie/ai/analysis-svc/helm-values.yaml
        workspaces:
          - name: shared-workspace
            emptyDir: {}
          - name: docker-credentials
            secret:
              secretName: zot-registry-credentials
