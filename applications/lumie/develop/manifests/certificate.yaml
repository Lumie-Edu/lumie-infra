# TLS Certificate for dev.lumie-edu.com
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lumie-dev-tls
  namespace: lumie-dev
spec:
  secretName: lumie-dev-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - dev.lumie-edu.com
